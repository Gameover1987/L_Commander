<mah:MetroWindow x:Class="L_Commander.App.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:L_Commander.App.Views"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:app="clr-namespace:L_Commander.App"
                 xmlns:controls="clr-namespace:L_Commander.App.Views.Controls"
                 xmlns:designMock="clr-namespace:L_Commander.App.Views.DesignMock"
                 mc:Ignorable="d"
                 Title="{x:Static app:Resources.MainWindowTitle}" 
                 Height="600" 
                 Width="800" 
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 ResizeMode="CanResizeWithGrip"
                 ShowIconOnTitleBar="True"
                 WindowStartupLocation="CenterScreen"
                 d:DataContext="{d:DesignInstance Type=designMock:DesignMockMainViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:QuickActionsControl Grid.Row="0" Margin="6,6,6,0"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <controls:FileManagerControl Grid.Column="0" Margin="0,12,0,0"
                                         DataContext="{Binding LeftFileManagerViewModel}"/>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="Transparent" ShowsPreview="True" Width="3" />

            <controls:FileManagerControl Grid.Column="2" Margin="0,12,0,0"
                                         DataContext="{Binding RightFileManagerViewModel}"/>
        </Grid>

        <controls:FileActionsControl Grid.Row="2"/>
    </Grid>
</mah:MetroWindow>
